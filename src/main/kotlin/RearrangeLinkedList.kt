open class LinkedList(value: Int) {
    var value = value
    var next: LinkedList? = null
}

fun rearrangeLinkedList(head: LinkedList, k: Int): LinkedList {

    // TODO: Rearrange linked list in such a way that node with values less than k should be on left of k and values greater than K should on right of
    // Todo Note: should be done in place
    return head
}
